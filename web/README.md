# Gopher Social — web

Activation UI. Run `npm run dev`. In development the client calls the API at `http://localhost:8080` unless `VITE_API_BASE_URL` is set (see `.env.example`). A Vite proxy for `/v1` is available if you switch the client to same-origin requests.
